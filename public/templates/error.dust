<!DOCTYPE html>
<html>
    <head>
        <script type="text/javascript">
            {#error}
                var err = '{error}';
                if(err){
                    err = err+'\n如有疑问请关注【润石创投】服务号进行反馈，我们会第一时间答复\n感谢您的支持和理解';
                    var url = '{url}';
                    alert(err);
                    setTimeout(function () {
                        if(!url){
                            history.back();
                        }else{
                            window.location = url;
                        }
                    }, 1000);
                }
            {/error}
        </script>

    </head>

    <body>
    </body>
</html>